# Keep the Docker build context explicit. The image is public, so never let
# local env files, git metadata, operator notes, or data files enter it.
*

!Dockerfile
!.dockerignore
!package.json
!yarn.lock
!tsconfig.json
!src/
!src/**
