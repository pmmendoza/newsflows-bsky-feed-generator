# Compliance Activity Endpoints

Status: compatibility pointer. The canonical endpoint contracts for
`GET /api/study/compliance-activity-summary`, `GET /api/compliance/activity`,
and the legacy engagement export live in:

`/Users/pm/Work/VUPD/projects/BSKY/docs/api_endpoints.md`

Use that root documentation for request parameters, response shapes, auth
classes, and smoke examples. Feedgen-local implementation notes can stay in
this repo, but endpoint reference material should not be duplicated here.
